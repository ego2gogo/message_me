<div class="event">
    <div class="content">
        <div class="summary">
            <%= message.created_at.strftime("%Y/%m/%d - %H:%M %Z") %> | <em style="color: red;"><%= message.user.username %> : </em> <%= message.body %>
        </div>
    </div>
</div>